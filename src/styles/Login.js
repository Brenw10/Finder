import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    container: {
        flex: 1
    },
    logo: {
        flex: 2,
        justifyContent: 'center',
        alignItems: 'center'
    }
})